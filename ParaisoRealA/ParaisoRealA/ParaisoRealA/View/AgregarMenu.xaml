<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ParaisoRealA.ViewModel"
             x:Class="ParaisoRealA.View.AgregarMenu">

    <ContentPage.BindingContext>
        <vm:AgregarMenuVM></vm:AgregarMenuVM>
    </ContentPage.BindingContext>
    
    <StackLayout Margin="20">
        <StackLayout>
            <Entry Text="Spiner que despliega las categorias"></Entry>
        </StackLayout>
        <ScrollView>
        <ListView>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell></ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
          
        </ScrollView>
        <StackLayout>
            <Button Text="Agregar Productos" Command="{Binding AgregarPCommand}"></Button>
        </StackLayout>
    </StackLayout>
   
</ContentPage>
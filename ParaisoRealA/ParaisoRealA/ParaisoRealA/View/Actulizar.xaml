<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParaisoRealA.View.Actulizar">

    <ContentPage.ToolbarItems>
        <ToolbarItem>
            <ToolbarItem.Icon>
                <FileImageSource File="upadate.png"></FileImageSource>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <StackLayout Margin="20">
        <StackLayout>
            <Label Text="Editar Menu Seleccionado" HorizontalOptions="Center" TextColor="Black"></Label>
            <Label Text="" Margin="0,20,0,0"></Label>
            <Entry Placeholder="Aqui va ir un spinner que despliegue"></Entry>
        </StackLayout>
        <ScrollView>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell></ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
        <StackLayout  Orientation="Horizontal" HorizontalOptions="Center">
            <Button Text="Actualizar" Command="{Binding ActulizarCommand}"></Button>
            <Button Text="Elminar" Command="{Binding EliminarCommand}"></Button>
        </StackLayout>
        </StackLayout>
</ContentPage>
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ParaisoRealA.ViewModel"
             x:Class="ParaisoRealA.View.DetalleMenus">

    <ContentPage.BindingContext>
        <vm:DetalleMenusVM></vm:DetalleMenusVM>
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem>
            <ToolbarItem.Icon>
                <FileImageSource File="combo.png"></FileImageSource>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <StackLayout Margin="20">
        <StackLayout>
            <Label Text="Ver Detalle del Menu Seleccionado" HorizontalOptions="Center" TextColor="Black"></Label>
        </StackLayout>
        <ScrollView>
            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell></ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollView>
        <StackLayout HorizontalOptions="Center">
            <Button Text="Editar" 
                    Command="{Binding EditarCommand}">
            </Button>
        </StackLayout>
    </StackLayout>
</ContentPage>